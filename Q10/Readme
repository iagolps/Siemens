Answer for question 10.

Whith this code I define a function stringLength that takes a const char* as input and iterates over each character of the string until it encounters the null terminator '\0'.